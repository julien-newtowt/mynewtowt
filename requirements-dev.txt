# NEWTOWT mynewtowt — development & test dependencies
-r requirements.txt

# Test
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-xdist==3.6.1
testcontainers[postgres]==4.9.1
aiosqlite==0.22.1  # driver async sqlite in-memory pour les tests unitaires
httpx==0.28.1
faker==33.3.0

# Lint / format
ruff==0.9.2
black==24.10.0
mypy==1.14.1
types-passlib==1.7.7.20241221
types-bleach==6.2.0.20241123

# Security scanners
# NB: `safety` retiré — il épingle pydantic<2.10 et rendait l'install CI
# impossible (ResolutionImpossible) face à pydantic==2.10.4 de l'app.
# L'audit de vulnérabilités passe par pip-audit, installé dans son propre
# step CI (cf. .github/workflows/ci.yml) pour ne pas contraindre ce venv.
bandit==1.8.2

# Coverage
coverage[toml]==7.6.10
